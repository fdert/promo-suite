# Never serve error logs over the web — they can contain file paths, SQL,
# and other internal details.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
